language: ruby
jdk:
- oraclejdk8
rvm:
- 2.3.1
install:
- rvm use 2.3.1
- gem install bundler
- bundle install
script: buildr package jacoco:report error_codes:check_duplicates
after_success:
- bash <(curl -s https://codecov.io/bash)
git:
  depth: false
